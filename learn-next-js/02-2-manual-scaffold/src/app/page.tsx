import { Button } from '@/components'

// React Server Component
export default function HomePage(params) {
  return (
    <section>
      <h1>홈 페이지</h1>
      <Button>로그인</Button>
    </section>
  )
}
